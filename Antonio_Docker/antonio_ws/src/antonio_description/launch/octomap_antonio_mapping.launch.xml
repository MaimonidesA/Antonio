<launch>
	<node pkg="octomap_server" exec="octomap_server_node" name="octomap_server">
		<param name="resolution" value="0.02" />

		<!-- fixed map frame (set to 'map' if SLAM or localization running!) -->
		<param name="frame_id" value="map" />

		<!-- maximum range to integrate (speedup!) -->
		<param name="sensor_model.max_range" value="1.0" />

		<param name="sensor_model.miss " value="0.001" />

		<param name="sensor_model.hit " value="0.007" />

		<!-- data source to integrate (PointCloud2) -->
		<remap from="cloud_in" to="/cloud" /> # cloud ni topic '/scan' for lidar or '/camera/depth/color/points' for realsens 
	</node>
</launch>
